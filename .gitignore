# Environment files — never commit keys or RPC URLs
.env
*.env
.env.*
!.env.example

# Build output and dependencies
node_modules/
dist/
*.tsbuildinfo

# TanStack Router generated route tree (regenerated by `pnpm routes` / `dev` / `build`)
src/routeTree.gen.ts

# OS
.DS_Store
Thumbs.db

# Editor workspace environment
.vscode/
.idea/
*.swp
*.swo
*~
.aider*
AGENTS.md

# Key material — belt and suspenders
*.pem
*.key
keystore/